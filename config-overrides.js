module.exports = function override(config, env) {
  // config.module.rules.push({ // add html loader required for tfc, scriptiq, cryptoiq
  //   test: /\.html/,
  //   exclude: /\.css/,
  //   use: [{ loader: 'html-loader' }]
  // });

  return config;
} 
